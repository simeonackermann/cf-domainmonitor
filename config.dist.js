
var config = {
    development: {
        apimail: "",
        apikey: "",
        limit: 10 // only load x entries to make the request faster
    },
    production: {
        apimail: "",
        apikey: "",
    }
};

module.exports = config;